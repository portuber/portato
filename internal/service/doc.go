// Package service installs and removes the OS-level autostart entry for
// `portato daemon` — a launchd LaunchAgent on macOS and a systemd user unit
// on Linux — behind a single Install/Uninstall interface.
package service
