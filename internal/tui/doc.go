// Package tui implements the Portato terminal UI on top of bubbletea v2: the
// tunnel list, the inline tunnel editor, the logs panel, the passphrase modal,
// and the standalone->daemon hand-off. It is driven by a controller.Controller
// and is agnostic of whether that controller is local or remote.
package tui
